package it.isw.cvmobile.widgets.slider.listeners;

import it.isw.cvmobile.utils.annotations.Completed;


@Completed
public interface OnImageChangedListener {
    void onImageChanged(String currentImageUrl);
}
